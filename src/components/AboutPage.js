import React from "react";

const CartPage = () => (
    <div>
        <h2>Shopping Cart</h2>
        <p>
            This app uses React, Redux & React Router w/ other libraries
        </p>
    </div>
);

export default CartPage
;